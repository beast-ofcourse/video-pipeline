{
  "name": "@beast-course/video-pipeline",
  "version": "0.1.0",
  "description": "Autonomous, production-grade video generation pipeline for OpenCode — scene graph engine, motion library, 5-agent orchestration, and 3-layer QA",
  "workspaces": [
    "packages/*"
  ],
  "bin": {
    "opencode-video-pipeline": "./bin/cli.js"
  },
  "main": "src/index.ts",
  "files": [
    "packages/",
    "src/",
    "bin/",
    "scripts/",
    "README.md"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "vitest run"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@remotion/bundler": "^4.0.0",
    "@remotion/renderer": "^4.0.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "remotion": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.10"
  },
  "peerDependencies": {
    "remotion": "^4.0.0",
    "react": "^18.0.0"
  }
}
